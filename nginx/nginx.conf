events {}

# TODO: изучить больше как писать конфиг ато этот вариант сгенерированый ии, и я 
# не могу его проверить что там написано все что мне нужно общие детали понимаю хорошо но мелькие и специфичные нет.

http {
    # эту часть я не понимаю
    map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
    }

    server {
        listen 80;

        # FRONTEND (Vite + WS)
        location / {
            proxy_pass http://frontend:80;

            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            proxy_set_header Host $host;
        }

        # BACKEND API
        location /api/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
        }

       location /ws {
            proxy_pass http://backend:8000;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }
}